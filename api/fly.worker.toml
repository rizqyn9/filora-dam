app = "filora-worker"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  ENVIRONMENT = "production"

[processes]
  worker = "/bin/worker"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
